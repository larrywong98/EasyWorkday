import React from "react";

const NextSteps = ({ status, idx }) => {
  const nextStep = () => {
    if (status == "pending") {
    }
    if (status == "approved") {
    }
  };
  return <div>NextSteps</div>;
};

export default NextSteps;

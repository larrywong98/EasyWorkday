import React from "react";

const UploadForm = () => {
  return (
    <div>
      <p>Upload File:</p>
      <input type="file" />
      <button>Upload</button>
    </div>
  );
};

export default UploadForm;
